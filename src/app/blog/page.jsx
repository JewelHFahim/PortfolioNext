import React from "react";
import styles from "./page.module.css";

export const metadata = {
  title: "Blog",
  description: "From Blog Page",
};

const Blog = async () => {
  return <div className={styles.title}>Coming Soon...</div>;
};

export default Blog;
